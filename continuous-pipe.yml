variables: []
defaults: []
tasks:
  images:
    build:
      services:
        app:
          image: gcr.io/quotatis-152617/demo-cp
          naming_strategy: sha1
          environment: []
  deployment:
    deploy:
      services:
        app:
          endpoints:
            - name: app
          deployment_strategy:
            readiness_probe:
              type: tcp
              port: 80
